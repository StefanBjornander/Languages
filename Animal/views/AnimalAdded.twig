{% block animalAdded %}
  <h1>Animal Added</h1>

  {% if ok %}
    <table border>
      <tr>
        <td class="column">Id:</td>
        <td class="column">{{id}}</td>
      </tr>
      <tr>
        <td class="column">Kind:</td>
        <td class="column">{{kindName}}</td>
      </tr>
      <tr>
        <td class="column">Name:</td>
        <td class="column">{{name}}</td>
      </tr>
      <tr>
        <td class="column">Farm:</td>
        <td class="column">{{farmText}}</td>
      </tr>

      <tr>
        <td colspan="2">
          <form action="/animalList" method="post">
            <input type="submit" style="width:100%" value="Animal List">
          </form>  
        </td>
      </tr>
    </table>
  {% else %}
    <p>An animal with identity number&nbsp;{{id}} does already exist.</p>

    <form action="/animalList" method="post">
      <input type="submit" value="Animal List">
    </form>  
  {% endif %}
{% endblock %}